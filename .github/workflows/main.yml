name: A workflow for my Hello World life 
on: push
jobs:
        build:
                name: Hello world action
                runs-on: ubuntu-latest
                steps:
                        - uses: actions/checkout@v1
                        - env:
                                FILE_PATH: action-a/Dockerfile
                        run:
                                docker build -f ${{ FILE_PATH }} .
